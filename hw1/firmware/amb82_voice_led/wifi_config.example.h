#pragma once

// Copy this file to wifi_config.h and enter the existing Wi-Fi credentials.
// Keep wifi_config.h private. The computer and AMB must share a reachable LAN.
#define WIFI_SSID "YOUR_WIFI_NAME"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
